{
    "type": "object",
    "properties": {
        "email": {
            "type": "string",
            "format": "email"
        },
        "password": {
            "type": "string",
            "minLength": 8,
            "description": "Must contain at least one letter and one number"
        }
    },
    "required": [
        "email",
        "password"
    ],
    "additionalProperties": false
}